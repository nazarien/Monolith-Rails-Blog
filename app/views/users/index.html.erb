<h1>Profiles</h1>

<% @users.each_with_index do |user, index| %>
    <tr>
      <%= index + 1 %>
      <%= ' -- '%>
      <td><%= user.email %></td>
      <%= '. Id:  '%>
      <td><%= user.id %></td>
      <br>
    </tr>
<% end %>
<%= link_to 'Home', root_path%>