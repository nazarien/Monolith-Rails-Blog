<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>

<p>
  <strong>Text:</strong>
  <%= @article.text %>
</p>

<%= link_to 'To articles', user_path(current_user.id)%>
<%= link_to 'Edit', edit_article_path(@article.id) %> |
<%= link_to 'Home', root_path%>
